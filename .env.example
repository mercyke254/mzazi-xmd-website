# ─────────────────────────────────────────────────────────────────────────────
# MZAZI XMD WEBSITE — environment
#
# Copy to .env.local and fill in.
#
# This site talks to the platform's Neon database DIRECTLY. There is no API key
# and no second database: pairing works because this site writes to the same
# `bot_control` queue the running WhatsApp bot polls, and signs people in against
# the same `users` table mzazi.shop uses.
#
# Only ONE value is required: DATABASE_URL. Everything else has a working default.
#
# NO QUOTES AROUND THE VALUES. A quoted value is the mistake that takes this site
# down: `"postgres://..."` arrives with the quote marks still in it, and the error
# you get back describes something else entirely. Leave them off. (If you are
# pasting into the Vercel dashboard, paste the value only — not `NAME=value`, and
# not the quotes.)
#
# After setting these on a deployment, open /api/health. It reports whether each
# one is set, whether the database answered, which key is signing sessions, and
# which tables it found — the whole diagnosis for "why can I not sign in".
# ─────────────────────────────────────────────────────────────────────────────

# REQUIRED. The platform's Neon connection string — the SAME database mzazi.shop
# and the bots use. In the Neon dashboard: Connection Details → the pooled
# connection string, which looks like
#   postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
#
# Do not point this at a new Neon project: sign-in would fail with a missing
# `users` table, because nothing would have created one.
DATABASE_URL=

# OPTIONAL — and leave it empty if you do not have the value to hand.
#
# Sessions are signed with a key. Without this variable the site generates one for
# itself on first use and keeps it in the `settings` table, so there is nothing to
# configure and nothing to remember. Sessions then work on this site only.
#
# Setting it (to the platform's value) makes the two sites share sessions: a
# session issued by either is accepted by both. Worth doing if you know it; not
# worth hunting for.
#
# Changing or clearing it signs everybody out once, and no more than that.
JWT_SECRET=

# The public address of THIS site, used for canonical URLs, the sitemap and social
# previews. Your Vercel address, or your own domain once it is pointed here.
NEXT_PUBLIC_SITE_URL=https://mzazi-xmd-website.vercel.app

# The main site, for the links this one does not re-implement: creating an
# account, plans and prices, the wallet.
NEXT_PUBLIC_MZAZI_SITE=https://www.mzazi.shop
